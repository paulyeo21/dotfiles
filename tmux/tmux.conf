# Ctrl-s prefix key. Default => C-b. Send C-s with repeat option via C-s C-s
unbind-key C-b
set -g prefix C-s
bind -r C-s send-prefix
set -g default-command "zsh"
set -g base-index 1
set -g renumber-windows on
set -g display-panes-time 350
# improve colors
set -g default-terminal 'screen-256color'
# Keep plenty of history for scrollback
set -g history-limit 10000
# Use emacs / readline key-bindings at the tmux command prompt `<prefix>:`
set -g status-keys emacs
# address vim mode switching delay (http://superuser.com/a/252717/65504)
set -s escape-time 0

# Simplify status bar display.
set -g status-left-length 40
set -g status-fg black
set -g status-bg green
set -g status-left '[#S] '
set -g status-right "#{?pane_synchronized,--SYNCED--,} #(is-online) #(battery -t -g black)  #(date '+%a, %b %d - %H:%M') "

# more intuitive keybindings for splitting
bind \ split-window -h -c "$PWD"
bind - split-window -v -c "$PWD"

# Reload tmux.conf with prefix-r
bind C-r source-file ~/.tmux.conf \; display "Reloaded ~/.tmux.conf"
